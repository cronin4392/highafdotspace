import React from 'react';

const Intro = ({}) => {
	return (
		<div className="intro">
			<a className="intro--cta" href="#app">
				<span>Updates</span>
			</a>
		</div>
	);
}

export default Intro;